# Copy to .env and fill in values
# Dev: use VPS Tailscale IP
# Prod (on VPS): use localhost

DATABASE_URL=postgresql://portfolio:password@<tailscale-ip>:5432/spx_interpolated

# Dashboard server
HOST=0.0.0.0
PORT=8000
