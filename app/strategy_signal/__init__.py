"""Strategy Signal: is each configured strategy eligible to enter right now?

library.py   the metrics a strategy can name, and their time series
evaluate.py  the decision -- pure, no database
store.py     the saved strategy configurations (JSONB)
"""
